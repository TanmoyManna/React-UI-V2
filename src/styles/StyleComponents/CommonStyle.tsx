import styled from "styled-components";

export const CommonSectionWrapper=styled.div`
    h2{
        font-size: 25px;
        font-weight: 800;
        line-height: 1.4;
        color: #000;
        margin-bottom: 10px;
        span{
            font-weight: 600;
            line-height: inherit;
            font-size:inherit;
            color: inherit;
            font-family: inherit;
        }
    }
    p{
        font-size: 16px;
        line-height:1.5;
    }
`